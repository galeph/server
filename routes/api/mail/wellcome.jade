extends out
block content
	table.twelve.columns: tr.render
		td.panel
			p= gettext('saludo %s', me.profile.name )
			.start!= gettext('wellcome texto inical')
			table.button: tr
				td: a( href= GLOBAL.CONFIG.servers.auth.url + me.linkActive() )= gettext('btn wellcome')
			.fainal!= gettext('wellcome texto final')